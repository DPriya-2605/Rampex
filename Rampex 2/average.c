#include <stdio.h>

int main() {
    int a=20,b=40,c=60,d=80,e=90;
    int total=a+b+c+d+e;
    printf("total=%d\n",total);
    int average=total/5;
    printf("average=%d\n",average);
    return 0;
}