#include <stdio.h>

int main() {
    int isprime=2,num;
    for(int i=2;i<=num/2;i++)
    {
        if(num%10==0)
         {
            is prime=0;
            printf("Is prime");
            break;
         }
         else{
             printf("Not a prime");
         }
    }
}